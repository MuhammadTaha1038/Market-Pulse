// Production environment configuration
// Set BACKEND_URL to your deployed backend domain before running: ng build --configuration=production
export const environment = {
  production: true,
  baseURL: 'https://your-production-domain.com', // ← replace with actual backend URL before deploying
  apiVersion: 'v1'
};
